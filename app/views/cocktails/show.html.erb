<h1>Details:</h1>
<h2><%= @cocktail.name %></h2>
<p><% @cocktail.doses.each do |dose| %></p>
  <p><%= dose.ingredient.name %> | <%= dose.description %> <%= link_to '🗑', dose_path(dose), method: :delete %></p>

   <!-- delete -->

<% end %>

<%= link_to "Add new dose", new_cocktail_dose_path(@cocktail) %>
<%= link_to("Back", cocktails_path) %>
